/**
 * Instant route-transition feedback while the server resolves the space.
 * Neutral surface so the swap to real content is calm.
 */
export default function Loading() {
  return <div className="min-h-screen" style={{ background: "#fafafa" }} />;
}
