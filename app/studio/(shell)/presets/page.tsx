/**
 * Presets — placeholder during the account-area UI rebuild.
 * Preset storage + API (/api/studio/presets) untouched; the editor UI is being
 * redesigned.
 */
export default function StudioPresetsPage() {
  return (
    <main className="flex min-h-[60vh] items-center justify-center p-10 text-center">
      <div>
        <p className="mono text-[11px] uppercase tracking-[0.22em] text-white/40">Studio · Presets</p>
        <p className="mt-3 text-[15px] text-white/50">Dieser Bereich wird neu aufgebaut.</p>
      </div>
    </main>
  );
}
