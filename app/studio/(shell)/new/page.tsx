/**
 * New project — placeholder during the account-area UI rebuild.
 * The creation API + classifier are untouched; the guided builder UI is being
 * redesigned (incl. prompt field, presets, fast-prompts, conditions).
 */
export default function NewProjectPage() {
  return (
    <main className="flex min-h-[60vh] items-center justify-center p-10 text-center">
      <div>
        <p className="mono text-[11px] uppercase tracking-[0.22em] text-white/40">Studio · Neues Projekt</p>
        <p className="mt-3 text-[15px] text-white/50">Dieser Bereich wird neu aufgebaut.</p>
      </div>
    </main>
  );
}
